# Project Context — SESSION CONTINUITY (compaction #34)

## Ловушки
- Целевые прогоны --tests; LSP ложные (FpgaBackend150; Exp002 107/117/134 — фикс toWords на месте); субагенты недоступны.
- Последний коммит 91e6d1b (запушен). Safety-guard блокирует delete-класс из агента — не обходить.

## Mission: волны по плану. Директива владельца: удалить models/ (диск+git).

## НОВЫЙ ФАКТ
`models/` уже НЕ существует на диске (workdir-clean), FREE_ROOT=39G — владелец выполнил удаление сам (мои команды из прошлого отчёта) либо внешняя очистка. Git-сторона требует финализации.

## ТЕКУЩИЙ ШАГ
1. `git status --short | head -15` + счётчик D по models/.
2. Если есть незастейдженные удаления tracked models-файлов: `git add -A` → commit «WAL: модели удалены владельцем с диска; сняты с индекса» → push.
3. Если чисто (владелец уже закоммитил) — только проверить log.
4. Sanity: ./gradlew :matrix-core:compileJava -q EXIT=0 (сборка не зависит от моделей).
5. WAL.md: обновить известные проблемы — «модели удалены владельцем (диск+git финализирован агентом); восстановление: история git до коммита удаления / scripts/pretrain_neurons.py».
6. git add WAL; если были изменения — commit+push; итоговый отчёт пользователю: место 39G свободно, git чист, очередь остатков (DJL dep, доменные данные, audio-events эт.3, алиасы, постквант v2, JMH-гейт Batch*, полные цепи Ханселя).

## Правила
FROZEN/avro/workflows не трогать; forbidden claims избегать.
