# Project Context — SESSION CONTINUITY (compaction #49) — DOC AUDIT

## Mission (владелец)
Проверить, вся ли документация полностью перенесена в docs-v2/ — ничего не осталось за кадром? Гипотезы, алгоритмы, требования, утверждения. Полный отчёт + уровень соответствия кода.

## ТЕКУЩИЙ ШАГ: разведка объёма
- ls docs-v2/archive/2026-08-pre-v2/ — полный листинг всех подпапок и файлов.
- cat подсчёт всех категорий (L*, idx_*, ss_*, gl_*, wave*, d13, AGENTS.md, и т.п.).
- grep по новым файлам docs-v2/ для ключевых терминов из старого (например «L0_manifesto», «dual», «OPERATOR_PROCEDURE», «Noosphere», «frozen_letters» и т.п.) — что из старого упомянуто/не упомянуто в новом.
- Выявить пропуски → решение для каждого: inline в существующий новый файл / создать новый / оставить в архиве.

## Затем
- Прогнать тесты: ./gradlew :matrix-core:test --tests "io.matrix.*" (целевой, как обычно) + compileJava чтобы доказать что код не сломан архивированием.
- Составить матрицу «старый doc ↔ где покрыт в docs-v2» с галочками ✓/❌.
- Финальный отчёт владельцу.

## Правила
FROZEN/avro/workflows не трогать; forbidden claims избегать.