# EXP-047 preregistration

## Hypothesis (H-047)
Cross-pillar latency budget split: perception < 5ms, deliberation < 50ms,
action < 10ms (p99, synthetic-scope).

## Setup
- ConsciousnessLoop orchestrator + ActionArena
- Empty BrcChain (identity) and uniform saliency
- 1000 ticks, single-thread, 100 iterations warmup
- Per-stage boundaries: tick total budget = 65ms p99

## Gate
- p99 per-stage ≤ target in 9/10 ticks
- Tick total p99 ≤ 65ms

## Status
**pending**