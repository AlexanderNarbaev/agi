# EXP-040 preregistration

## Hypothesis (H-040)
M2→M3 promotion criteria: prediction-error > δ AND integrity-check
pass. Promotion precision ≥ 0.9 (synthetic-scope).

## Setup
- MaturityGateKeeper with synthetic promotion criteria
- 100 episodes; each has GateCriteria with (predictionError, integrityPass)
- criteria: PE > 0.5 AND integrity = true → approve
- ground truth: same criterion (oracle)
- precision = approved ∧ correct / total approved

## Gate
- Promotion precision ≥ 0.9

## Status
**pending**