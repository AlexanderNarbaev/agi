# WAL 396 — End-to-End Tests for Real Conversation

**Date:** 2026-09-19
**Branch:** `main`
**Focus:** Edge case + integration tests for the conversation CLI stack
**Checkpoint Hash:** `01bd9491`
**Previous Checkpoint:** `01bd9491` (W395)

## Objective
Add comprehensive JUnit tests that exercise edge cases in the CLI stack.

## File: `RealConversationEndToEndTest.java`

8 tests (all passing):

1. `testListSessionsReturnsNonNull` — basic list works
2. `testListSessionsExcludesHiddenFiles` — filter logic verified
3. `testReplayClassExists` — RealConversationReplay loads
4. `testJsonFieldExtractionHandlesEmptyString` — edge case
5. `testJsonFieldExtractionHandlesEscapes` — escape handling
6. `testJsonFieldExtractionHandlesNewlines` — newline handling
7. `testListSessionsWithZeroLimit` — boundary case
8. `testListSessionsWithNegativeLimit` — boundary case

## Notes

- Tests do NOT include actual Qwen model inference (too slow for CI)
- Model inference is verified manually with `RealConversationCli` runs
- Edge cases target the JSON parser which is most error-prone

## CONSTITUTION Compliance

- **Article VI:** Robust parsing for measurement substrate

## Files Added

- `matrix-core/src/test/java/io/matrix/cli/e2e/RealConversationEndToEndTest.java`
- `docs-v2/waves/WAL-396.md`

---

**Next Wave:** W397 (Run native build to verify CLI commands still work)
