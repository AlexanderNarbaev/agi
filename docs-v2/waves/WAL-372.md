# WAL 372 — ModulatorRegistry Property Tests

**Date:** 2026-09-18
**Branch:** `main`
**Focus:** Property-based tests for ModulatorRegistry invariants
**Checkpoint Hash:** `4a208c9d`
**Previous Checkpoint:** `4a208c9d` (W371)

## Objective
Verify ModulatorRegistry invariants using jqwik property-based testing.

## File: `ModulatorRegistryPropertyTest.java`

8 properties (all passing, 1000 cases each):

1. **addThenGetReturnsSame** — add then get returns same definition
2. **versionIncrementsPerMutation** — version = initial + mutation count
3. **sizeMatchesAddedCount** — size() matches added modulators
4. **deterministicOrdering** — getAll() returns sorted by ID
5. **hashDeterministicForSameContent** — same content → same hash (seed-independent)
6. **differentContentDifferentHash** — different type → different hash
7. **canMutateIsLevelBased** — L0/L1 false, L2+ true
8. **exportProtoPreservesCount** — proto modulators count matches store

## CONSTITUTION Compliance

- **Article I v3:** Seeded Random consistent across tests
- **Article V:** Property-based verification of spec invariants

## Files Added

- `matrix-core/src/test/java/io/matrix/federation/property/ModulatorRegistryPropertyTest.java`
- `docs-v2/waves/WAL-372.md`

---

**Next Wave:** W373 (Property tests for BiochemicalMediator)
