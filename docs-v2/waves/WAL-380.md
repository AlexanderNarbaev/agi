# WAL 380 — Federation CLI Commands

**Date:** 2026-09-18
**Branch:** `main`
**Focus:** CLI commands for federation operations in native binary
**Checkpoint Hash:** `eceba66b`
**Previous Checkpoint:** `eceba66b` (W379)

## Objective
Expose federation runtime features via CLI in the native binary.

## New CLI Commands

### `--federation`
Prints federation runtime demo summary.

### `--federation-modulators`
Lists sample modulator definitions.

### `--federation-vote`
Demonstrates a voting scenario.

## Implementation

Added to `io.quarkus.runner.ApplicationImpl.handleCommand()`:
- New case statements in switch
- Three new methods: `runFederationDemo()`, `runFederationModulators()`, `runFederationVote()`

## Native Build Verification

- Binary size: 126MB (unchanged)
- Build time: 55s
- `--federation`: prints 8-class summary
- `--federation-modulators`: prints 4 modulators table
- `--federation-vote`: prints voting scenario

## CONSTITUTION Compliance

- **Article VI:** CLI is observation/control interface, no subjective state

## Files Modified

- `matrix-core/src/main/java/io/quarkus/runner/ApplicationImpl.java`
- Native binary: `matrix-core/build/native/nativeCompile/matrix-core`

---

**Next Wave:** W381 (Telemetry exposure via Prometheus format)
