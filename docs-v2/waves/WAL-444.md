# WAL 444 — Docker Build Helper CLI

**Date:** 2026-09-19
**Branch:** `main`
**Focus:** Generate Dockerfile and docker-compose for conversation stack
**Checkpoint Hash:** `66e37b46`
**Previous Checkpoint:** `66e37b46` (W443)

## Class: `io.matrix.cli.ConversationDocker`

### Verified
```
$ java io.matrix.cli.ConversationDocker
Generated Docker files in: docker

Files created:
  - docker/Dockerfile
  - docker/docker-compose.yml
  - docker/.dockerignore
  - docker/README.md

Next steps:
  1. cd docker
  2. cp ../../matrix-core/build/libs/matrix-core-1.0.0.jar build/libs/
  3. cp -r ../../models ./
  4. docker compose build
  5. docker compose up -d
```

### Files Generated
- `docker/Dockerfile` - Java 21 JRE + matrix-core.jar + Qwen model
- `docker/docker-compose.yml` - Service with 3 ports + 2 volumes
- `docker/.dockerignore` - Build exclusions
- `docker/README.md` - Setup instructions

### Endpoints
- `http://localhost:9093/` - Web UI
- `http://localhost:9093/chat` - Standard chat
- `http://localhost:9094/stream` - SSE streaming
- `http://localhost:9095/` - Test/eval

## CONSTITUTION Compliance

- **Article V:** Deployment spec

## Files Added

- `matrix-core/src/main/java/io/matrix/cli/ConversationDocker.java`
- `docker/Dockerfile`
- `docker/docker-compose.yml`
- `docker/.dockerignore`
- `docker/README.md`
- `docs-v2/waves/WAL-444.md`

---

**Next Wave:** W445 (Continue with more features)
