# WAL 454 — Conversation to SQL CLI

**Date:** 2026-09-19
**Branch:** `main`
**Focus:** NDJSON to SQL migration tool
**Checkpoint Hash:** `02f38502`
**Previous Checkpoint:** `02f38502` (W453)

## Class: `io.matrix.cli.ConversationToSql`

### Verified
```
$ java io.matrix.cli.ConversationToSql /tmp/conversations.sql
Exported 18 sessions, 249 turns to /tmp/conversations.sql

$ head -20 /tmp/conversations.sql
-- MATRIX Conversation SQL Export (W454)

CREATE TABLE IF NOT EXISTS conversations (
  session_id VARCHAR(128) PRIMARY KEY,
  name VARCHAR(256),
  ...
);
```

### Features
- Generates `conversations` table (session summary)
- Generates `turns` table (individual messages)
- Auto-detects META name from comments
- Skips archived sessions
- Proper SQL escaping (single quote doubling)
- 305 lines for 249 turns

### Usage
```
java io.matrix.cli.ConversationToSql [output-file]
Default: data/exports/conversations.sql
```

## CONSTITUTION Compliance

- **Article V:** Data format spec (SQL migration)

## Files Added

- `matrix-core/src/main/java/io/matrix/cli/ConversationToSql.java`

---

**Next Wave:** W455 (Continue with more features)
