# WAL 371 — TLA+ Config + Java Model Checker

**Date:** 2026-09-18
**Branch:** `main`
**Focus:** TLA+ configuration + Java-based model checking
**Checkpoint Hash:** `b2702091`
**Previous Checkpoint:** `b2702091` (W370)

## Objective
Add TLA+ configuration file for TLC verification and a Java-based
model checker that verifies the same invariants on the actual implementation.

## File 1: `docs-v2/tla/FederationConsensus.cfg`

TLC model checker configuration:
- SPECIFICATION Spec
- Constants: L0-L7 with voting weights, STANDARD_THRESHOLD
- Symmetry permutation for state space reduction
- 5 INVARIANTs (Safety, TypeOK, VetoInvariance, ApprovalThreshold, WeightMonotonic, VetoOverridesApproval)
- PROPERTY SafetySpec

## File 2: `matrix-core/src/test/java/io/matrix/federation/formal/ConsensusModelCheckerTest.java`

Java-based bounded model checker (8 tests, 0 failures):

1. **exhaustiveTraces** — bounded exhaustive exploration (100s of traces)
2. **randomTraceInvariants** — 1000 random traces verify invariants
3. **approvalThresholdProperty** — APPROVED ⟹ ratio ≥ threshold
4. **weightMonotonicProperty** — total = YES + NO weights
5. **vetoAlwaysOverrides** — any VETO ⟹ VETOED status
6. **emptyEngineIsPending** — no votes = PENDING
7. **singleL7VetoIsVetoed** — single L7 VETO sufficient
8. **determinismProperty** — same seed + operations = same result

## TLC Status

- TLC NOT installed locally (no `tlc` binary in PATH)
- Spec + config documented for future verification
- Java model checker complements TLA+ by verifying same invariants on actual impl

## To install TLC

```bash
# Download TLA+ Toolbox or tla2tools.jar from:
# https://github.com/tlaplus/tlaplus/releases
# Or use Java directly:
java -jar tla2tools.jar FederationConsensus.tla -config FederationConsensus.cfg
```

## CONSTITUTION Compliance

- **Article II:** Formal verification of consensus properties
- **Article V:** Spec-driven, TLA+ spec precedes implementation

## Files Added

- `docs-v2/tla/FederationConsensus.cfg` (TLC config)
- `matrix-core/src/test/java/io/matrix/federation/formal/ConsensusModelCheckerTest.java`
- `docs-v2/waves/WAL-371.md`

---

**Next Wave:** W372 (Property-based tests for ModulatorRegistry)
