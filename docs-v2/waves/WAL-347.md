# WAL 347 — ProtoBuf Schema and SPEC-013 Dynamic Modulator Registry

**Date:** 2026-09-18
**Branch:** `main`
**Focus:** Foundation wave — ProtoBuf v3 schema for federation + Dynamic Modulator Registry specification
**Checkpoint Hash:** 430de111
**Previous Checkpoint:** 19800125 (W346)

## Objective
Establish the data contract for liquid federation by:
1. Defining ProtoBuf v3 messages for modulators, neurons, consensus, GPU, ethics, and WAL
2. Writing SPEC-013 with functional and non-functional requirements
3. Defining FROZEN constraints tied to CONSTITUTION Articles

## Artifacts (committed in `44d6bd7d`)

### 1. ProtoBuf Schema (`proto/matrix_federation_v1.proto`)
- 481 lines, package `matrix.federation.v1`, Java package `io.matrix.federation.proto`
- Messages: `ModulatorRegistry`, `ModulatorDefinition`, `NeuronBlock`, `FederationNode`,
  `ConsensusProposal`, `GpuTask`, `FrozenFilter`, `WaveCheckpoint`, `TelemetryEvent`
- Enums: `ModulatorType` (6 types), `CapabilityLevel` (L0-L7), `NodeType` (6 roles),
  `VoteDecision` (incl. VETO), `FilterMode`, `EthicalPrinciple` (7), `SeverityLevel` (4)
- No gRPC service blocks yet (deferred to implementation wave)

### 2. SPEC-013 (`docs-v2/specifications/SPEC-013-dynamic-modulator-registry.md`)
- Functional R1-R7: consensus-only CRUD, capability access, FROZEN zones, polymorphic types,
  versioning + Merkle root, Redis cache, PostgreSQL CQRS/Event Sourcing
- Non-functional NF1-NF5: <1ms cache / <10ms DB read latency, 99.9% availability,
  5s consensus timeout, 10k modulators max, ProtoBuf v3+ compat
- 3-tier architecture: Node Local Cache (Redis) → Consensus Engine (Pekko) → Global Store (PG)
- FROZEN constraints (3): `ethical_no_harm_threshold`, `consensus_min_threshold`, `capability_progression_rate`
- 4 implementation phases: Core (W352-W355), Consensus (W356-W360), Integration (W361-W365), Testing (W366-W370)

## CONSTITUTION Compliance
- **Article II** (Consensus): voting weights encoded in .proto
- **Article IV** (Safety): `FrozenFilter` + `EthicalPrinciple` enum + `SafetyConstraints`
- **Article V** (Spec-driven): SPEC-013 precedes implementation
- **Article VI** (no consciousness claim): measurements are deployment substrates only

## Issues Resolved
1. Fixed hormone count (7 → extensible dictionary)
2. Replaced fixed node roles with liquid federation
3. Added FROZEN zone enforcement at ProtoBuf level

## Blockers
None.

## Next Steps
W348 — DESIGN-67 Liquid Federation Architecture.

---

**Next Wave:** W348
