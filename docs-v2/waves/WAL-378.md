# WAL 378 — Registry Round-Trip Serialization Tests

**Date:** 2026-09-18
**Branch:** `main`
**Focus:** ProtoBuf round-trip verification
**Checkpoint Hash:** `4dd4f893`
**Previous Checkpoint:** `4dd4f893` (W377)

## Objective
Verify ModulatorRegistry ProtoBuf serialization preserves all data.

## File: `RegistryRoundTripTest.java` (under serialization/)

7 tests (0 failures):

1. **testSingleModulatorRoundTrip** — add → export → import preserves all fields
2. **testHashInvariantUnderRoundTrip** — hash identical after round-trip
3. **testMultipleModulatorsRoundTrip** — 4 modulators of different types
4. **testVersionPreservedAfterRoundTrip** — version in proto matches
5. **testEmptyRegistryRoundTrip** — empty round-trips correctly
6. **testFROZENPreserved** — FROZEN constraints survive round-trip
7. **testProtoSerializationPreservesBytes** — bytes → parse → restore

## CONSTITUTION Compliance

- **Article V (Spec-driven):** ProtoBuf is the spec, verified

## Files Added

- `matrix-core/src/test/java/io/matrix/federation/serialization/RegistryRoundTripTest.java`
- `docs-v2/waves/WAL-378.md`

---

**Next Wave:** W379 (Snapshot/Restore pattern)
