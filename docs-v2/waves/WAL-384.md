# WAL 384 — Minor Issue Fixes (REVIEW-CYCLE-3 Followup)

**Date:** 2026-09-18
**Branch:** `main`
**Focus:** Address minor issues from goal-reviewer
**Checkpoint Hash:** `fe16908b`
**Previous Checkpoint:** `fe16908b` (W383)

## Objective
Fix the minor issues noted in goal-reviewer's audit of W356-W382 code.

## Fixes Applied

### 1. RegistryBackup.verify stricter validation
- **File:** `RegistryBackup.java`
- **Issue:** Accepted ANY non-empty file as valid backup
- **Fix:** Reject empty files AND require `version > 0` or `modulators > 0`

### 2. PrometheusExporter escape label values
- **File:** `PrometheusExporter.java`
- **Issue:** Labels with `\n`, `\`, or `"` would corrupt Prometheus output
- **Fix:** Added `escapeLabel()` helper following Prometheus spec
  - `\\` → `\\\\`
  - `\n` → `\\n`
  - `\"` → `\\\"`

### 3. testRngDeterministic misnamed
- **File:** `FederationTelemetryTest.java`
- **Issue:** Test name suggests "same seed → same value" but actually tests different seeds
- **Fix:** Renamed to `testRngDifferentSeedsDiffer`

### 4. Vacuous VetoInvariance check in model checker
- **File:** `ConsensusModelCheckerTest.java`
- **Issue:** Checked `boolean hasVeto = false` then `assertFalse(hasVeto)` which is always true
- **Fix:** Removed the vacuous check (status VETOED is the observable proxy)

## Verification

- 190 tests pass, 0 fail
- No regressions in existing tests

## CONSTITUTION Compliance

- **Article VI:** Escape labels prevent output corruption (Prometheus spec compliance)

## Files Modified

- `matrix-core/src/main/java/io/matrix/federation/backup/RegistryBackup.java`
- `matrix-core/src/main/java/io/matrix/federation/telemetry/PrometheusExporter.java`
- `matrix-core/src/test/java/io/matrix/federation/formal/ConsensusModelCheckerTest.java`
- `matrix-core/src/test/java/io/matrix/federation/telemetry/FederationTelemetryTest.java`
- `docs-v2/waves/WAL-384.md`

---

**Next Wave:** W385 (Final cumulative review)
