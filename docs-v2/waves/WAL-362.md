# WAL 362 — GpuTaskExecutor Stub

**Date:** 2026-09-18
**Branch:** `main`
**Focus:** GPU task executor stub with CPU fallback
**Checkpoint Hash:** `f44bfc01`
**Previous Checkpoint:** `f44bfc01` (W361)

## Objective
Implement `GpuTaskExecutor` that accepts `GpuTask` ProtoBuf messages and produces
`GpuResult` outputs. Real GPU dispatch (CUDA/OpenCL/ROCm) deferred — current impl
provides CPU fallback for testing.

## Class: `io.matrix.federation.gpu.GpuTaskExecutor`

### GpuStats Record

```java
public record GpuStats(
    Map<GpuOperation, Integer> operationCounts,
    Map<GpuOperation, Long> operationTimeNanos,
    int totalTasks,
    int successCount,
    int timeoutCount,
    int oomCount
) {}
```

### Methods

- `execute(GpuTask)` — process task, return GpuResult
- `getStats()` — cumulative execution statistics
- `isGpuEnabled()` — true if GPU available, false for CPU fallback
- `getTotalTaskCount()` — convenience accessor
- `getRng()` — deterministic random

### Status Values (per .proto)

- `GPU_STATUS_SUCCESS` — operation completed
- `GPU_STATUS_RUNNING` — currently in progress (not used in stub)
- `GPU_STATUS_FAILED` — error during execution
- `GPU_STATUS_TIMEOUT` — exceeded timeout
- `GPU_STATUS_OOM` — out of memory

### Custom Exceptions

- `TimeoutException` (RuntimeException) — simulated kernel timeout
- `OutOfMemoryException` (RuntimeException) — simulated CUDA OOM

### CONSTITUTION Compliance

- **Article I v3:** Seeded Random exposed via `getRng()`
- **Article VI:** Stats are numerical, no subjective state

## Tests: `GpuTaskExecutorTest` (10 tests, 0 failures)

- testBasicExecution — happy path
- testAllOperationsSupported — 6 GPU operation types
- testNullTaskThrows — input validation
- testStatsUpdate — counts/time per operation
- testCpuFallbackMode — gpuEnabled = false
- testGpuModeFlag — gpuEnabled = true
- testExecutionTimeRecorded — elapsed nanos
- testTotalTaskCount — task counter
- testRngAccessible — same seed → same value
- testEchoOutputPreservesInput — input == output for stub

## Files Added

- `matrix-core/src/main/java/io/matrix/federation/gpu/GpuTaskExecutor.java`
- `matrix-core/src/test/java/io/matrix/federation/gpu/GpuTaskExecutorTest.java`
- `docs-v2/waves/WAL-362.md`

---

**Next Wave:** W363 (Telemetry hooks)
