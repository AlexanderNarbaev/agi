# WAL 387 — Edge Case Tests + NPE Fixes

**Date:** 2026-09-18
**Branch:** `main`
**Focus:** Edge case tests revealed NPE issues in null inputs
**Checkpoint Hash:** `9bbb09c9`
**Previous Checkpoint:** `9bbb09c9` (W386)

## Objective
Add edge case tests for federation classes and fix NPE issues found.

## Edge Case Tests (14 new tests)

### ModulatorRegistryStore
- `testModulatorRegistryAddNullThrows` — null def rejected
- `testModulatorRegistryAddEmptyIdThrows` — empty ID rejected
- `testModulatorRegistryAddDuplicateThrows` — duplicate ID rejected
- `testModulatorRegistryRemoveNonExistentThrows` — non-existent ID rejected
- `testModulatorRegistryUpdateNonExistentThrows` — update non-existent rejected

### LocalConsensusEngine
- `testConsensusAddNullVoteThrows` — null vote rejected
- `testConsensusEngineCreateVoteNullLevelThrows` — null level returns 0 weight

### FederationRuntime
- `testFederationRuntimeNullCapabilityThrows` — null capability rejected (FAIL-10)
- `testFederationRuntimeProposeAddNullDef` — null def returns empty
- `testFederationRuntimeProposeAddZeroCapability` — L0 cannot mutate
- `testFederationRuntimeRemoveNonExistentReturnsFalse` — non-existent returns false

### Mediator/Bridge
- `testBiochemicalMediatorNullRuntimeThrows` — null runtime rejected
- `testBiochemicalMediatorNullMediatorThrows` — null mediator rejected
- `testCognitiveModulationBridgeNullProfileThrows` — null profile rejected

## NPE Fixes Discovered

### 1. LocalConsensusEngine.getVotingWeight
- **Bug:** `level.getNumber()` NPE if level is null
- **Fix:** Added null check returning 0.0

### 2. FederationRuntime.proposeAdd
- **Bug:** `def.hasSafety()` NPE if def is null
- **Fix:** Added null check returning Optional.empty()

### 3. FederationRuntime.proposeUpdate
- **Bug:** Same as proposeAdd
- **Fix:** Added null check

### 4. FederationRuntime.proposeRemove
- **Bug:** Null id would propagate to registry.remove(null)
- **Fix:** Added null/empty check

## Verification

```bash
./gradlew :matrix-core:test --tests "io.matrix.federation.*"
```

**Result: 208 PASS, 0 FAIL** (+14 from previous 194)

## CONSTITUTION Compliance

- **Article IV (Safety):** Null inputs handled gracefully, no NPEs

## Files Modified

- `matrix-core/src/main/java/io/matrix/federation/consensus/LocalConsensusEngine.java`
- `matrix-core/src/main/java/io/matrix/federation/runtime/FederationRuntime.java`
- `matrix-core/src/main/java/io/matrix/federation/integration/CognitiveModulationBridge.java`
- `matrix-core/src/test/java/io/matrix/federation/edge/FederationEdgeCaseTest.java` (new)
- `docs-v2/waves/WAL-387.md`

---

**Next Wave:** W388 (Additional hardening — concurrent modification tests)
