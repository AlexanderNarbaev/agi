# WAL 390 — SHA-256 Hash Upgrade (FAIL-6 followup)

**Date:** 2026-09-18
**Branch:** `main`
**Focus:** Upgrade consensus hash from XOR to SHA-256
**Checkpoint Hash:** `cac8ca1b`
**Previous Checkpoint:** `cac8ca1b` (W389)

## Objective
Address the deferred FAIL-6 recommendation from REVIEW-CYCLE-3:
upgrade ModulatorRegistryStore.calculateHash() from XOR-based hash to
SHA-256 cryptographic hash.

## Changes

### ModulatorRegistryStore.calculateHash()

**Before:** XOR-based 64-bit hash
- Not collision-resistant
- Not cryptographic
- 16 hex chars

**After:** SHA-256 cryptographic hash
- Collision-resistant
- Cryptographically secure
- 64 hex chars
- Used MessageDigest.getInstance("SHA-256")

### New Helper: intToBytes(int)
- Converts int to 4-byte big-endian byte array
- Required for SHA-256 digest update

## Verification

```bash
./gradlew :matrix-core:test --tests "io.matrix.federation.*"
```

**Result: 216 PASS, 0 FAIL**

The pre-existing DecentralizedDigestPipelineTest (which was failing earlier
in this session) now passes — likely flaky timing issue resolved.

## CONSTITUTION Compliance

- **Article IV (Safety):** Cryptographic integrity (collision-resistant hash)
- **Production Readiness:** SHA-256 meets industry standard

## Files Modified

- `matrix-core/src/main/java/io/matrix/federation/registry/ModulatorRegistryStore.java`
- `docs-v2/waves/WAL-390.md`

---

**Next Wave:** W391 (Comprehensive final verification)
