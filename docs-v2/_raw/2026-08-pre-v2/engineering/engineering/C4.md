# C4-модель MATRIX/MENTAT

**Статус: living** · Архитектурные виды по модели C4 (Браун): контекст → контейнеры → компоненты → (код — DESIGN-01…10). Диаграммы — Mermaid (рендерятся на GitHub). Трассировка: каждый блок ссылается на дизайн-документ, требования (GOALS-REQUIREMENTS) и основание в атласе (SYSTEM-SYNTHESIS). При противоречии нормативны DESIGN-документы; этот файл — карта, не спека.

## Уровень 1. Контекст системы

```mermaid
flowchart TB
    owner["Владелец<br/>(человек, ноутбук/смартфон)"]
    teacher["TeacherOracle<br/>(человек-учитель в интерактивном обучении)"]
    mentat["MENTAT-узел<br/>локальный контур: восприятие → размышление → действие<br/>BIR-ядро, без GPU, без обязательного LLM"]
    rag["rag-system<br/>(внешняя зависимость, ADR-001)"]
    llm["Локальный LLM ≤3B<br/>(опциональный бэкенд сигналов и baseline экспериментов)"]
    pool["Noosphere-пул<br/>P2P-федерация узлов: gossip + CRDT + консенсус"]
    edge["Эдж-профили EDGE-1…3<br/>смартфон → носимые → (перспектива) имплант"]

    owner -->|запросы, golden-наборы, аудит| mentat
    teacher -->|уроки, подтверждения Φ-гейту| mentat
    mentat -->|контрактные запросы (изоляция по ADR-001)| rag
    mentat -->|медиа-линейки через SignalModule| llm
    mentat -->|обезличенный экспорт (k=100 + DP, opt-in)| pool
    pool -->|свежие нейроны через M4-гейт| mentat
    mentat -->|делегирование рефлексов| edge
```

Ключевые границы контекста:
- LLM — **опция, а не ядро** (G-B); все внешние зависимости изолированы контрактами (DESIGN-03 §5, ADR-001).
- Экспорт в пул — только обезличенный и opt-in (DESIGN-08 §4, H-013); персональные полезные данные узел не покидают (tombstone-политика, CONSTITUTION).
- Учитель — первоклассный участник: интерактивное обучение с подтверждением (DESIGN-04 §4, «три соединения» HWME, атлас §24).

## Уровень 2. Контейнеры узла

```mermaid
flowchart TB
    subgraph node["MENTAT-узел (JVM, Java 25; native — GraalVM, JAVA_NATIVE)"]
        api["Фасад API<br/>OpenAI-совместимый REST + прокси + MCP<br/>DESIGN-03 §6"]
        pipeline["Контур запроса<br/>PERCEPTION → DELIBERATION → RENDERING<br/>BRC ≤5 шагов с witness · DESIGN-03"]
        reflex["ReflexLayer<br/>«мозжечковый» слой µs-рефлексов<br/>SUBSTRATE-MODELS §5.1"]
        core["BIR-ядро<br/>единицы TT/CLAUSESET/BDD, компилятор форм, верификатор<br/>DESIGN-01/02 · SPEC-002"]
        memory["Память M0–M4<br/>слои, recall, консолидация (сон), tombstone<br/>DESIGN-05"]
        reservoir["Бинарный резервуар<br/>VSA-состояние 1 КБ, TT/WNN-readout<br/>DESIGN-10 · H-015"]
        learn["Офлайн-обучение<br/>TsetlinTrainer, WNN/TREPAN, MonotoneDecoder<br/>DESIGN-04/09"]
        cauldron["Cauldron (самосоздание)<br/>ряды МГУА + Φ-гейт + бюджетер<br/>FNL, TaskCell · DESIGN-07"]
        federation["Федерация и безопасность<br/>ELSP, эдж-профили, экспорт в пул<br/>DESIGN-08"]
        guard["FROZEN-слой<br/>Четыре запрета, hash-locked<br/>CONSTITUTION"]
    end
    signals["Модули сигналов<br/>SignalModule + ModuleRegistry (frozen)<br/>DESIGN-06"]
    ext["Внешний мир<br/>владелец, LLM, rag-system, пул"]

    ext --> api --> pipeline
    signals --> pipeline
    pipeline --> reflex
    reflex --> core
    pipeline --> core
    core --> memory
    pipeline --> memory
    reservoir --> core
    learn -->|кандидаты через Φ-гейт| core
    cauldron -->|рождённые BirNet/FNL через карантин| learn
    cauldron --> memory
    memory -->|CLS-цикл: эпизоды → корпус| learn
    federation -->|M4-гейт импорта| core
    api --> federation
    guard -.->|вето над любым артефактом| core
    guard -.->|вето над экспортом| federation
```

Замечания по контейнерам:
- **Все стрелки в core проходят верификатор**: артефакт без заголовка (provenance, fidelity, Φ, хэш) не исполняется (SPEC-002, FR-02).
- **FROZEN-слой — не контейнер, а сквозное вето** (пунктир): реализован как hash-locked BIR-артефакты + проверка в контуре (CONSTITUTION, Запреты 1–4).
- Cauldron и обучение работают только в офлайн-окнах и proactive-слотах — онлайн-контур не блокируется (DESIGN-07 §5, NFR-10).

## Уровень 3. Компоненты: BIR-ядро

```mermaid
flowchart LR
    subgraph core["BIR-ядро (DESIGN-01/02)"]
        unit["BirUnit<br/>TT (k≤20, long[]-LUT) /<br/>CLAUSESET / BDD<br/>контракт evaluate"]
        compiler["Компилятор форм<br/>нормальные алгоритмы Маркова<br/>локальные правила (Генцен)"]
        verifier["Верификатор<br/>BDD-эквивалентность O(1),<br/>заголовок provenance/Φ/хэш"]
        net["BirNet (DAG)<br/>топологическая оценка,<br/>композиция как SMC (4 правила)"]
        cap["Capability<br/>BirNet + ioSchema + метрики"]
        view["Точка зрения<br/>ансамбль + роутер + калибратор"]
        persona["Профессионал (Persona)<br/>сертифицированная точка зрения MA-3+"]
    end
    unit --> compiler --> verifier
    verifier --> net --> cap --> view --> persona
    net -->|BRC-шаги, witness| pipeline_out["контур запроса"]
```

Основания: TT-санкция — Мойсил (§26); каноничность BDD ≡ автоустойчивость (§8); изотропия CLAUSESET — Пирс (§31); композиция SMC — §2; локальность правил компилятора — Генцен (§33); wp-вывод сторожей — Дейкстра (§28).

## Уровень 3. Компоненты: Cauldron и гейты

```mermaid
flowchart TB
    subgraph cauldron["Cauldron (DESIGN-07)"]
        rows["Ряды кандидатов<br/>нарастающая сложность (МГУА-стиль)<br/>генераторы: ГУХА H-018 · AQ/LAD-звезда H-020"]
        sched["Бюджетер рядов<br/>расписание Левина 2^−l + Kt (H-019)<br/>сопряжённый DP + теневые цены ψ (H-021)"]
        gate["Φ-гейт<br/>монотонный функционал, MDL-рачет<br/>трёхзначный вердикт accept/reject/undecided"]
        quarantine["Карантин FNL<br/>теневой прогон, promote/demote через M4-гейты"]
        arena["Off-heap арена на цикл<br/>пулинг, без GC-штормов"]
        cells["TaskCell<br/>эфемерные задачные инстансы<br/>INV-TC1/2/3"]
    end
    rows --> sched --> gate --> quarantine
    arena -.-> rows
    gate -->|откат при ухудшении Φ| rows
    quarantine -->|рождённый BirNet/FNL + lineage| registry["Реестр элементов"]
    cells -->|итог в общий пул| registry
```

Контур самонаблюдения (гомеостат, H-022) стоит над контейнерами: метрики (энергия/тик, покрытие, доля UNDECIDED, отказы гейтов) → коридоры → минимальная коррекция из версионируемого меню (перебалансировка бюджетов, понижение фрагмента); каждая коррекция — событие с witness (EXP-022).

## Уровень 4

Код-уровень не дублируется здесь: структуры данных, псевдокод, сложности, бюджеты и приёмочные тесты — в DESIGN-01…DESIGN-10 (порядок чтения — SYSTEM-SYNTHESIS §6). Правило: компонент на этой карте существует только если у него есть DESIGN-документ; новый компонент = новый DESIGN + ADR.

## Развёртывание

```mermaid
flowchart TB
    subgraph laptop["EDGE-0: ноутбук владельца (полный контур)"]
        jvm["JVM-узел<br/>Java 25 / GraalVM native"]
        k8s["Kubernetes Operator (CRD)<br/>режимы: активен/спит/консолидирует/карантин<br/>DESIGN-07 §7"]
        jvm --- k8s
    end
    subgraph phone["EDGE-1: смартфон"]
        slim["Урезанный контур<br/>рефлексы + избранные Capabilities<br/>тот же BIR-артефакт"]
    end
    subgraph wear["EDGE-2/3: носимые / (перспектива) имплант"]
        ref["Только ReflexLayer + делегирование<br/>тот же BIR-артефакт"]
    end
    laptop -->|ELSP: Ed25519 + подписи + anti-replay| phone
    phone -->|ELSP| wear
```

Один BIR-артефакт исполняется на всех профилях (FR-12, NFR-09); режимы узла — Statecharts (§13), инварианты INV-S1/S2 проверяются model checking'ом в CI (H-017).

## Соглашения диаграмм

- Сплошная стрелка — синхронный вызов/поток данных; пунктир — сквозное ограничение (вето, политика, бюджет).
- Каждый блок обязан трассироваться: DESIGN-документ → FR/NFR → (при наличии) гипотеза H-xxx и раздел атласа.
- Изменение топологии контейнеров — только через ADR (см. ADR-001…005) и обновление этого файла в том же коммите.
