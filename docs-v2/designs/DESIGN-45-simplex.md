# DESIGN-45 — Simplex / Linear Programming (Dantzig 1947)

> RUN 396. From OR — solves max c·x s.t. Ax ≤ b, x ≥ 0 in polynomial
> time. Extends ConjugateBudgeter to continuous domain. Enables
> fractional budget allocation across chains, ShadowPrice λ
> computation in conjugate mode.
EOF