# DESIGN-44 — A* Search (Hart 1968)

> RUN 394. From AI planning — best-first graph search with
> f(n) = g(n) + h(n). Complete + optimal given admissible heuristic.
> Pairs with existing Ac3Solver (constraint preprocessing) and
> MultiChainEnsemble (chain selection).
EOF