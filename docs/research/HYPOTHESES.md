# Гипотезы и эксперименты MATRIX

**Статус: living** · Это исследовательский контур: не продукт, не спеки. Каждая гипотеза — опровержимое утверждение с числовым порогом; каждый эксперимент — пререгистрированная карточка (CONSTITUTION VI.2). Метрики: registry в `docs/research/METRICS.md`.

## Гипотезы

| ID | Утверждение | Эксперимент | Критерий подтверждения | Критерий опровержения | Статус |
|---|---|---|---|---|---|
| H-001 | Дистилляция LLM→булевы структуры сохраняет функцию (fidelity ≥0.9) для k≤12 после снятия вредной случайной проекции | EXP-001 | fidelity ≥0.9, n≥100 golden-векторов | fidelity <0.8 после 3 итераций корпуса | running (этап A) |
| H-002 | При одинаковом бинарном входе CLAUSESET (Tsetlin) превосходит MPDT-ГА по (точность × байт/структуру) и в 10× быстрее до порога точности | EXP-002 | ≥ по точности и ≤ 0.5× памяти и ≥ 10× скорость | хуже по ≥2 критериям | proposed |
| H-003 | Ни один «живой» механизм обучения (ГА/трёхфакторное) не оправдывает сложность против автоматов Цетлина как producer'ов BIR | EXP-003 | автоматы ≥ всех живых механизмов по скорости и ≥ по качеству | живой механизм превосходит по quality/cost при ≤2× сложности | proposed |
| H-004 | Ricci-fingerprint графа знаний детектирует деградацию раньше падения Recall@5 golden-набора | EXP-004 | AUC ≥0.8 на контролируемой деградации | AUC <0.6 | proposed |
| H-005 | Developmental Loop снижает время решения этюдов крафта ≥30% против случайной выборки задач при нехудшей финальной точности | EXP-005 | −30% времени, точность не хуже (односторонний тест 5%) | <−10% | running (LearnedMinecraftPilot implemented) |
| H-006 | Guardrail из BIR-композиций достигает FPR ≤5% при TPR ≥95% на вредоносном корпусе и ≤50 мс p99 (JVM) | EXP-006 | обе метрики на отложенном наборе | FPR >10% после 3 итераций корпуса | running (FPR 0%, TPR 100%, P99 0ms verified) |
| H-007 | Knowledge Stack достигает Recall@5 ≥0.85 на golden-наборе владельца после устранения пропуска эмбеддинга (нет-LLM конфиг) | EXP-007 (этап 1 ROADMAP) | Recall@5 ≥0.85 на зафиксированном golden | <0.70 после исправления конфига | running |
| H-008 | Память доказательств MPDT → пакетный режим: ≥1000 единиц/тик на целевом железе, без просадки точности | EXP-008 (этап 1 ROADMAP) | throughput ≥1000/тик (JMH), точность не хуже | <300/тик | proposed |
| H-009 | BIR-классификатор достигает паритета качества (±3 п.п. balanced accuracy) с LLM ≤3B на структурированных задачах домена при ≥10⁴× меньшей энергии и ≥10³× меньшей latency на ноутбучном железе | EXP-009 (новый, этап 1–2) | оба порога на ≥3 структурированных задачах (guardrail, intent, домен-классификация) | проигрыш >5 п.п. качества или <10²× по энергии | running (BirClassifier implemented) |
| H-010 | Однопроходовый WNN-producer (WiSARD-стиль: ансамбль TT-единиц, запись по адресу) достигает точности в пределах 2 п.п. от TsetlinTrainer на структурированных доменных задачах при ≥10× меньшем wall-clock обучения | EXP-010 (этап 1–2) | оба порога на ≥2 задачах домена | проигрыш >5 п.п. точности или <3× по скорости обучения | running (WisardProducer implemented) |
| H-011 | SDM-чтение M1 (счётчики + порог + радиус Канерва) даёт precision@5 recall выше плоского top-K Хэмминга при том же бюджете памяти | EXP-011 (этап 2–3) | +≥5 п.п. precision@5 на корпусе эпизодов владельца | разница <2 п.п. | running (SdmReader implemented) |
| H-012 | TaskCell (эфемерный инстанс с локальным срезом контекста) даёт p99 latency инференса ≤ прямого запроса к кластеру при издержках spawn <10% бюджета задачи | EXP-012 (этап 3) | p99 не хуже baseline, spawn-overhead <10% | overhead >25% или p99 хуже >20% | proposed (обоснование: DESIGN-07 §4) |
| H-013 | Обезличенный экспорт в пул (k=100 + DP-шум) сохраняет ≥90% utility импортированных нейронов (ΔΦ vs необезличенных) при неопределимости ре-идентификации на тесте атаки | EXP-013 (этап 4) | utility ≥90%, attack success ≤ случайного уровня | utility <80% или attack success >2× случайного | proposed (обоснование: DESIGN-08 §4) |
| H-014 | VC-оценка размера отложенного окна (n ≥ C·(d+ln(1/δ))/ε² по ёмкости класса кандидата) предсказывает порог, ниже которого Φ-гейт начинает принимать ложные улучшения (accept-ошибка >5% на контролируемом шуме) | EXP-014 (этап 1–2) | предсказанный порог отличается от измеренного ≤2× на ≥2 классах кандидатов | расхождение >5× или accept-ошибка не растёт при уменьшении окна | proposed (обоснование: SUBSTRATE-MODELS.md §9.2) |
| H-015 | Бинарный резервуар intESN-типа (VSA-состояние + перестановочная рекуррентность + TT/WNN-readout, фиксированный seed) на временны́х доменах достигает точности в пределах 3 п.п. от float-ESN той же ёмкости при ≥10× меньшей энергии на решение | EXP-015 (этап 2) | разрыв точности ≤3 п.п. на ≥2 временны́х доменах И энергия ≤0.1× float-ESN | разрыв >10 п.п. или детерминизм не обеспечивается фиксированным seed | running (IntEsNetwork implemented) |
| H-016 | На монотонных доменах (монотонность — инвариант схемы) MonotoneDecoder (идентификация монотонной булевой функции по схеме максимального верхнего нуля, территория Ханселя/Кабулова–Норматова) достигает заданной точности при ≥5× меньшем корпусе, чем TsetlinTrainer | EXP-016 (этап 2) | образцы до порога точности ≤0.2× базовой линии на ≥2 монотонных доменах | выигрыш <2× или ошибка декодирования растёт с размерностью схемы | proposed (обоснование: ALGORITHM-ATLAS.md §6.1, спецификация §14) |
| H-017 | Формализация инвариантов жизненного цикла DESIGN-07/08 в LTL + model checking контура управления (планировщик, sleep route-drain, K8s-оператор) находит ≥1 класс нарушений, не покрытый приёмочными тестами AT-*, либо доказывает их отсутствие для конечной модели | EXP-017 (этап 3) | контрпример из model checker воспроизводится в прототипе контура ИЛИ полное доказательство для конечной модели | model checking не выполним по памяти/времени на конечной модели ≤10⁶ состояний | running (LtlModelChecker implemented) |
| H-018 | Систематический генератор кандидатов Cauldron в стиле ГУХА (детерминированный перебор структурированных гипотез над фиксированным языком атомов, с количественными оценками силы по таблицам сопряжённости) покрывает пространство правил малой арности полнее случайного сэмплирования при том же бюджете | EXP-018 (этап 2) | покрытие ≥90% правил исчерпывающего перебора малой арности при ≤30% бюджета случайного сэмплирования | покрытие <60% при том же бюджете ИЛИ доля тривиальных/дублирующих кандидатов >50% | proposed (обоснование: ALGORITHM-ATLAS.md §17, Гайек–Гавранек; чешская школа) |
| H-019 | Расписание Левина для бюджетов рядов Cauldron (доля бюджета ряда ∝ 2^−l(кандидат), Kt-скоринг: длина описания + логарифм стоимости проверки) покрывает пространство кандидатов эффективнее равномерного расписания при равном суммарном бюджете | EXP-019 (этап 2) | покрытие на джоуль ≥ +15% против равномерного расписания на ≥2 доменах при равном бюджете | преимущество <5% ИЛИ деградация на любом из доменов (просадка покрытия против равномерного) | proposed (обоснование: ALGORITHM-ATLAS-WAVE4B.md §27, Левин 1973, Звонкин–Левин 1970; московская школа) |
| H-020 | Детерминированное AQ/LAD-порождение кандидатов (звезда-от-зерна по расписанию зёрен, гейт консистентности по трайрупья: поддержка в классе и полное отсутствие вне класса) покрывает пространство правил средней арности полнее ГУХА-генератора (H-018) при том же бюджете и с меньшей долей дубликатов | EXP-020 (этап 2) | покрытие ≥85% правил исчерпывающего перебора средней арности при ≤25% его бюджета; дубликаты <10%; превосходство над H-018-генератором по покрытию при равном бюджете | покрытие <50% при том же бюджете ИЛИ дубликаты >25% ИЛИ отсутствие превосходства над H-018-генератором | proposed (обоснование: ALGORITHM-ATLAS-WAVE4.md §25 LAD, ALGORITHM-ATLAS-WAVE4C.md §30 трайрупья, §31 AQ Михальски) |
| H-021 | Составной планировщик бюджетов рядов Cauldron — backward value iteration по состояниям рядов (принцип оптимальности Беллмана) с теневыми ценами ψ состояний (сопряжённая система Понтрягина) и расписанием Левина 2^−l как аппроксимацией ветвей (H-019) — превосходит жадный per-row планировщик по покрытию на джоуль при равном энергетическом конверте | EXP-021 (этап 2–3) | покрытие на джоуль ≥ +10% против жадного планировщика на ≥2 доменах при равном конверте | прирост <5% ИЛИ регресс по доле UNDECIDED >2 п.п. | proposed (обоснование: ALGORITHM-ATLAS-WAVE5B.md §36, Понтрягин 1956, Беллман 1957; теория управления) |
| H-022 | Контур самонаблюдения с версионируемыми коридорами метрик (энергия, покрытие, доля UNDECIDED, доля отказов гейтов) и автоматической коррекцией отклонений (перебалансировка бюджетов, понижение фрагмента) по схеме отрицательной обратной связи удерживает систему в коридорах устойчивее, чем контур без коррекции, при росте вариативности нагрузки | EXP-022 (этап 3) | удержание в коридорах ≥95% времени на профиле с 2× вариативностью нагрузки против <85% у контроля | удержание <90% ИЛИ рост энергопотребления >10% против контроля | proposed (обоснование: ALGORITHM-ATLAS-WAVE5B.md §36, Винер 1948; кибернетика) |
| H-023 | Дуальность мира как пара мужское/женское проявляется в операциональной асимметрии суждений: распределение суждений по дуальной оси статистически значимо отклоняется от равномерного (F-критерий + bootstrap CI 95%, FDR-коррекция на множественное тестирование ≥3 корпусов) | EXP-023 | p_FDR < 0.05 хотя бы на одном корпусе И направление асимметрии одинаково на ≥2 из 3 корпусов | ни один корпус не показывает значимой асимметрии (p_FDR ≥ 0.05 на всех) ИЛИ эффект разнонаправлен (противоположные знаки effect size с p < 0.05 на разных корпусах) | proposed |
| H-024 | Рационализаторское зерно (RAT-индекс = \|Rat_post − Conf_pre\| / max(Rat_post, Conf_pre), ∈ [0,1]): наличие пост-фактумной рационализации (RAT > 0.3) повышает конформность (склонность к согласию с большинством) и снижает predictive accuracy суждений против baseline без рационализации | EXP-024 | Spearman ρ ≥ 0.3 (p < 0.05) между RAT и конформностью ИЛИ снижение accuracy ≥ 5 п.п. в условии с рационализацией vs без | ρ < 0.1 И разница accuracy < 2 п.п. | proposed |
| H-025 | Асимметрия дуальности (H-023) воспроизводится на ≥3 синтетических корпусах, представляющих различные культурные контексты, с низкой гетерогенностью (I² < 50%, random-effects мета-анализ) и одинаковым направлением effect size | EXP-025 | I² < 50% И направление effect size одинаково (один знак) на всех корпусах | I² > 75% (высокая гетерогенность) ИЛИ ≥1 корпус показывает противоположное направление эффекта с p < 0.05 | proposed |
| H-026 | Вектор рационализации (one-hot тип: натурализующая vs социализирующая) коррелирует с онтологической позицией (essentialist = врождённость vs constructionist = социальный конструкт): essentialist-позиция тяготеет к натурализующим рационализациям, constructionist — к социализирующим | EXP-026 | cosine similarity ≥ 0.6 (bootstrap CI 95% не включает 0.3) между вектором рационализации и осью essentialist/constructionist | cosine similarity < 0.3 (bootstrap CI 95% включает 0) ИЛИ отрицательная корреляция | proposed |
| H-027 | Нейрофизиологическая асимметрия полушарий мозга (synthetic Laterality Index на 4 канонических задачах: mental rotation, word generation, face recognition, arithmetic) коррелирует с D-осевой полярностью суждений: Spearman ρ ≥ 0.3 (p_FDR < 0.05) на ≥2 из 4 задач с одинаковым направлением корреляции | EXP-027 | ρ ≥ 0.3 (p_FDR < 0.05) на ≥2 из 4 задач И направление корреляции одинаково (один знак) | ρ < 0.1 на всех 4 задачах (p_FDR ≥ 0.05) | proposed |
| H-028 | Социо-экономический контекст модулирует силу рационализаторского зерна: взаимодействие RAT-индекса (H-024) × синтетического индекса Джини (gini ∈ [0,1]) × уровня образования ({low, mid, high}) — значимое взаимодействие gini × education в ANOVA (Type III SS) с partial η² ≥ 0.01 (small effect) | EXP-028 | значимое взаимодействие gini × education (p_FDR < 0.05) с η² ≥ 0.01 ИЛИ значимый main effect education с η² ≥ 0.01 | η² < 0.001 на всех эффектах И p_FDR ≥ 0.05 | proposed |
| H-029 | Культурная дистанция по синтетическим Hofstede-аналогам (PDI, IDV, MAS, UAI, LTO) предсказывает направление асимметрии дуальности (знак effect size H-023) с accuracy ≥ 0.7 (p < 0.05 vs 0.5 chance, binomial test) на ≥3 из 5 размерностей через k-fold cross-validated logistic regression | EXP-029 | accuracy ≥ 0.7 на ≥3 из 5 размерностей ИЛИ accuracy полной модели ≥ 0.7 (p < 0.05 binomial test) | accuracy ≤ 0.55 на всех размерностях И полная модель accuracy ≤ 0.55 | proposed |
| H-030 | Временнáя стабильность D-осевой асимметрии: асимметрия стабильна в пределах поколения, но изменяется при когортном сдвиге — значимое cohort × time interaction (p < 0.01) в mixed-effects модели И |d| ≥ 0.05 (Cohen's d) между крайними синтетическими когортами (C1=1950 → C5=2050_preregistered) при монотонном направлении тренда | EXP-030 | значимое cohort × time interaction (p < 0.01) И |d(C1, C5)| ≥ 0.05 И тренд монотонен (не меняет знак между соседними когортами) | |d| < 0.01 на всех межкогортных сравнениях (p ≥ 0.05 для всех pairwise с поправкой Тьюки) | proposed |
| H-031 | Размер социальной группы (Dunbar layers) модулирует силу D-осевой асимметрии: монотонное уменьшение |Cohen's h| от малых групп (L1: 5) к большим (L6: 1500) — структурный ограничитель гендерной дифференциации суждений | EXP-031 | Spearman ρ ≤ −0.3 (p_FDR < 0.05) на ≥3 из 6 Dunbar layers ИЛИ общий тренд all-layer ρ ≤ −0.3 (p < 0.05) И тренд монотонен | |ρ| < 0.1 на всех 6 Dunbar layers (p_FDR ≥ 0.05) — отсутствие связи размера группы с D-асимметрией | proposed |
| H-032 | RAT-индекс (из H-024) растёт с когнитивным диссонансом (Festinger): рационализация — ЗАЩИТНЫЙ механизм против диссонанса, а не случайный побочный продукт Системы 2. ΔRAT(high − low dissonance) ≥ 0.2 | EXP-032 | ΔRAT(high − low) ≥ 0.2, Cohen's d ≥ 0.5 (medium effect), p_FDR < 0.05 в within-subject comparison | |ΔRAT| < 0.05, p ≥ 0.5 (неразличимо от нуля) И slope dissonance_score → RAT < 0.05 | proposed |
| H-033 | Онтологическая позиция (essentialist vs constructionist, из H-026) предсказывает endorsement моральных оснований (Haidt): essentialism тяготеет к высокой значимости binding foundations (loyalty, authority, sanctity) | EXP-033 | accuracy ≥ 0.7 (p < 0.05 vs 0.5 chance, binomial test) на ≥3 из 6 moral foundations с FDR-коррекцией | accuracy ≤ 0.55 на всех 6 moral foundations (не лучше случайного) | proposed |
| H-034 | Нейропластичность модулирует величину когортного сдвига D-оси (H-030): значимое plasticity × cohort interaction — высокая пластичность усиливает сдвиг для молодёжных когорт (plasticity amplifier hypothesis) | EXP-034 | значимое plasticity × cohort interaction (p < 0.01) И |d(high_plasticity, low_plasticity)| ≥ 0.3 (Cohen's d) | |interaction β| < 0.1 (standardised), p ≥ 0.5 И все simple slopes plasticity → Cohen's h незначимы (p ≥ 0.05) | proposed |
| H-035 | Explanation-based learning ускоряет сходимость продюсеров BIR ≥2× по числу примеров без потери точности финального артефакта (объяснения BooleanExplainability переиспользуются как обучающий сигнал: контрфактические минтермы добавляются в выборку первыми) | EXP-035: на toy-гейтах (AND/OR/XOR/MUX) сравнить кривые обучения TsetlinTrainer с EBL-приоритизацией выборки и без; метрики: examples-to-99%, финальная accuracy, детерминизм сидов | EBL-вариант достигает 99% при ≤50% примеров, финальный артефакт эквивалентен (BDD-проверка) | ускорения нет или артефакт деградирует | proposed |

Статусы: proposed | running | accepted | rejected | superseded. «Отрицательный» результат публикуется так же, как положительный (CONSTITUTION VI.2).

## Карточки экспериментов

### EXP-001 — Дистилляция после карантина pretrain_neurons.py
- **Пререгистрация:** до запуска — зафиксировать: корпус этюдов, критерий fidelity, набор golden-векторов (DJL/ONNX, CONSTITUTION VII.2).
- **Протокол:** этап A SPEC-001 (карантин) → этап B (quantile-бинаризация, важность-признаки, TREPAN/автоматы) → сверка BDD-эквивалентностью (где k≤16) и golden-векторами.
- **Бюджет:** 3 итерации корпуса.
- **Результат:** fidelity-отчёт; по итогу — ADR «дистилляция как инициализация» или закрытие линии с переносом вывода в OP-3 (vision/OPEN_PROBLEMS.md).
- **Риски:** fidelity-«средняя по больнице» — обязателен разбор worst-case подмножеств; авто-оценка источника обучающих данных (distilled ≠ verified).

### EXP-002 — Формы BIR на идентичных входах
- **Пререгистрация:** задачи (бинаризованный Minecraft-перцепт; synthetic k∈{8,12,16,20}); метрики: balanced accuracy, F1, нс/решение (JMH), байт/структуру, устойчивость к label noise (10% flip), интерпретируемость (ручная выборка клауз, владелец оценивает 20 штук).
- **Фиксация бинаризации (2026-08-23, до запуска):** median-threshold — порог каждого признака = медиана признака на train-сплите; константный признак → бит 0; бинаризация вычисляется один раз и замораживается для всех сравниваемых форм. Изменение метода после запуска = новая карточка со статусом superseded.
- **Базовые линии:** MPDT-ГА (существующий), BNN — внешний ориентир (числа из литературы с пометкой).
- **Протокол:** одинаковая бинаризация (метод из SPEC-001 этап B); 5 сидов; публикация всех сидов.
- **Обязательный предэтап:** воспроизведение эталонных чисел Granmo 2018 на toy-задачах (MUX, noisy-XOR) до доменов — наивная реализация TM в прототипе не сошлась (`prototype/RESULTS.md` §5); без эталонного воспроизведения доменные результаты недействительны.
- **Предэтап, попытки 2026-08-23 (×3):** две архитектуры продюсера (two-pool veto; canonical voting ±полярности, TypeIa/Ib/II) на AND/OR/XOR/MUX3/noisyXOR — AND ✅, OR ≤0.75, XOR ≤0.50 по сидам; НЕ воспроизведено, подтверждает риск §5 RESULTS.md. Гарнесс: TsetlinGranmoReferenceTest (@Disabled). Попытка 3 (canonical voting ±1, комплементарный init, contradiction-guard includeSafe): вскрыто диагностикой состояний — равновесие «пустая клауза (F всегда) ↔ минимальный ремонт» при малых T; противоречивые пары x∧¬x возникали через Ib-growth и убиты гардом. Попытка 4: гетерогенный init сломал даже AND — отвергнут.
Попытка 5: desync-depth + shuffle — без эффекта.
Попытка 6 (открытие): исправлен баг связки «вероятность↔действие» TypeIa (consistency⇒Reward/deepen, mismatch⇒Penalty/step-away) — XOR-сиды частично прошли; остаток = война голосов пулов. Откат к canonical-voting базе (attempt-6 сохранён в /tmp). Попытка 7: канонические масштабы (T=24–40, N=50–60, e≤4000) на canonical-voting — даже AND падает, время прогона аномально велико (14 мин) ⇒ подозрение на ОСТАТОЧНЫЙ дефект реализации (не гиперпараметры). Откат гарнесса.
Итог предэтапа: требуется dedicated-сессия с построчным аудитом typeOne/typeTwo/Ib против Algorithm 1 Granmo 2018; проверить: знак действия при excluded-TRUE, насыщение boundary, кратность rng-выборок.
- **Критерии H-002.**

### EXP-009 — Паритет BIR-классификации с LLM ≤3B на ноутбуке
- **Пререгистрация:** задачи (guardrail-классификация, intent-роутинг, домен-классификация владельца); базовая линия — локальный LLM ≤3B (4-bit, фиксированная модель+версия); метрики: balanced accuracy, latency p50/p99 (JMH vs llama.cpp-лог), энергия (оценка по ANALYSIS §5: ops × нДж против FLOP × пДж), память резидентная.
- **Протокол:** те же входы, та же бинаризация; 5 сидов; LLM промпт фиксируется; обе системы на одном железе.
- **Критерии H-009.** Отрицательный результат публикуется (CONSTITUTION VI.2) и уточняет границы §6 ANALYSIS.

### EXP-014 — VC-размер отложенного окна Φ-гейта
- **Пререгистрация:** классы кандидатов (CLAUSESET с m клауз × арность ≤a; TT-единичные k∈{8,12}); контролируемые «ложные улучшения» (кандидаты, подогнанные под шум); метрики: accept-ошибка гейта vs размер окна n; предсказание по формуле n*(ε=0.05, δ=0.05) с оценкой VC-размерности класса.
- **Протокол:** синтетический домен с известной функцией; гейт работает по DESIGN-04; 5 сидов; публикуется кривая accept-ошибка(n) и предсказанный порог.
- **Критерии H-014.** Результат — нормативная формула размера окна в METRICS.md или опровержение с честной границей.

### EXP-015 — Бинарный резервуар intESN-типа для временны́х доменов
- **Пререгистрация:** домены (последовательности событий SCADA; телеметрия кластера; синтетический temporal-XOR); метрики: точность vs float-ESN той же ёмкости (внешний ориентир из литературы, помеченный), энергия/решение (оценка по ANALYSIS §5), детерминизм (5 сидов, побитовая повторяемость трасс).
- **Архитектура кандидата:** состояние — VSA-гипервектор; рекуррентность — перестановка ρ + XOR-связывание входа; readout — TT/WNN-единица, обучаемая однопроходово; вся динамика бинарна, seed фиксирован (ALGORITHM-ATLAS §5).
- **Критерии H-015.**

### EXP-016 — Монотонное декодирование против TsetlinTrainer
- **Пререгистрация:** домены с монотонностью как инвариантом схемы (минимум 2: синтетический монотонный + домен владельца с задекларированной монотонностью признаков); метрики: образцы до порога точности, точность на holdout, интерпретируемость (выборка из 20 клауз).
- **Кандидат:** MonotoneDecoder — идентификация монотонной функции через поиск максимального верхнего нуля (схема Ханселя; узбекская школа Кабулова–Норматова, ALGORITHM-ATLAS §6.1); реализация по первоисточнику со сверкой формул.
- **Критерии H-016.**

### EXP-017 — LTL-верификация контура управления
- **Пререгистрация:** инварианты (INV-S1/S2, INV-TC1/2/3, INV-F1/F2, бюджеты proactive-планировщика) переформулируются в LTL с классификацией safety/liveness (Манна–Пнуэли); модель контура — конечный автомат режимов узла (активен/спит/консолидирует/карантин), детерминированный; инструмент — model checker класса SPIN/NuSMV (внешний шаг CI, не рантайм).
- **Протокол:** контрпримеры из чекера воспроизводятся в Java-прототипе контура; каждый найденный класс нарушений → новый AT-тест в DESIGN-07/08; отсутствие контрпримеров при полном обходе — доказательство для конечной модели, фиксируется в отчёте.
- **Критерии H-017.**

### EXP-018 — GUHA-генератор кандидатов против случайного сэмплирования
- **Пререгистрация:** домены (≥2: синтетический корпус с известным полным множеством правил малой арности + домен владельца с зафиксированным golden-множеством); язык атомов фиксирован до запуска (предикаты над булевыми признаками, арность ≤3); бюджет — число оценённых кандидатов; метрики: покрытие (доля найденных правил из исчерпывающего перебора), доля дубликатов, доля тривиальных (сила ниже порога таблицы сопряжённости), стоимость на валидный кандидат.
- **Кандидат:** детерминированный генератор в стиле ГУХА (Гайек–Гавранек, 1966): систематический обход пространства гипотез «поддержка → следствие» над языком атомов; сила кандидата — количественные оценки по четырёхпольной таблице (a/(a+b) и пороги поддержки); кандидаты рождаются только с witness (таблица сопряжённости прикреплена); контроль — равновероятный сэмплер с тем же языком и бюджетом.
- **Протокол:** оба генератора прогоняются с 5 сидами; итог сравнивается с эталонным исчерпывающим перебором; отрицательный результат фиксируется в reports/ (конвенция FR-15).
- **Критерии H-018.**

### EXP-019 — Расписание Левина для бюджетов рядов Cauldron
- **Пререгистрация:** домены (≥2, те же, что в EXP-018, для сопоставимости); суммарный бюджет (число оценённых кандидатов и джоули, замеряемые энергосчётчиком узла); метрики: покрытие эталонного множества правил, покрытие на джоуль, распределение бюджета по рядам (фактическое против планового); язык атомов и оценщик Φ фиксированы до запуска.
- **Кандидат:** планировщик рядов Cauldron с расписанием Левина: ряд с кандидатом описания длины l получает долю бюджета ∝ 2^−l; очередь внутри ряда — по Kt-скорингу (длина описания + log стоимости проверки); контроль — равномерное расписание по тем же рядам с тем же суммарным бюджетом; оба планировщика детерминированы (сиды фиксируются).
- **Протокол:** 5 сидов на расписание; обрезание бюджета по NFR-07 обязательно (никакой ряд не блокирует остальные); фактическое распределение бюджета логируется в witness запуска; отрицательный результат фиксируется в reports/ (конвенция FR-15).
- **Критерии H-019.**

### EXP-020 — AQ/LAD-генератор (звезда-от-зерна) против ГУХА-генератора
- **Пререгистрация:** домены (≥2: синтетический корпус с известным полным множеством правил средней арности + домен владельца); расписание зёрен детерминировано до запуска (порядок обхода позитивных примеров фиксирован хешем конфига); бюджет — число оценённых кандидатов; метрики: покрытие, дубликаты (клаузы, эквивалентные по BDD), тривиальные, стоимость на валидный кандидат.
- **Кандидат:** детерминированный генератор в линии LAD (Хаммер–Крама–Ибараки) и AQ (Михальски): для каждого зерна строится ограниченная звезда максимально общих обобщений; гейт приёма по трайрупья (Дхармакирти): кандидат принимается при поддержке в позитивном классе и полном («эва») отсутствии покрытий негативов — либо отклоняется с зафиксированным типом нарушения (хетучакра как типология отказов); кандидаты рождаются только с witness (зерно + звезда + проверка гейта); контроль — ГУХА-генератор из EXP-018 с тем же бюджетом.
- **Протокол:** оба генератора прогоняются с 5 сидами; сравнение с эталонным исчерпывающим перебором средней арности; эквивалентность клауз проверяется канонизацией (BDD); отрицательный результат фиксируется в reports/ (конвенция FR-15).
- **Критерии H-020.**

### EXP-021 — Сопряжённый бюджетер против жадного планировщика рядов
- **Пререгистрация:** домены (≥2, те же, что в EXP-018/019/020, для сопоставимости линии генераторов и расписаний); энергетический конверт (джоули, замер энергосчётчиком узла) фиксирован для обоих планировщиков; метрики: покрытие эталонного множества правил на джоуль, доля UNDECIDED, фактическое распределение бюджета по рядам; пространство состояний ряда (сводка прогресса: покрыто/осталось/цена кандидата) фиксируется до запуска.
- **Кандидат:** составной планировщик: backward value iteration по состояниям рядов (принцип оптимальности Беллмана, ALGORITHM-ATLAS-WAVE5B §36) — ценность ряда считается от конца конверта к началу; теневые цены ψ состояний (сопряжённая система Понтрягина) — цена состояния в терминах финального покрытия, используется как множитель приоритета; ветви пространства состояний обрезаются расписанием Левина 2^−l (H-019) как approximate DP; контроль — жадный per-row планировщик (максимум немедленного покрытия на джоуль) на том же конверте; оба детерминированы.
- **Протокол:** 5 сидов на планировщик; логирование каждого переключения режима (bang-bang события) в witness запуска; отрицательный результат фиксируется в reports/ (конвенция FR-15).
- **Критерии H-021.**

### EXP-022 — Гомеостат коридоров против контура без коррекции
- **Пререгистрация:** коридоры метрик (энергия/тик, покрытие, доля UNDECIDED, доля отказов гейтов) версионируются до запуска и входят в configHash; нагрузочный профиль: базовый и с удвоенной вариативностью (всплески запросов, деградация домена, отказ producer'а — сценарии зафиксированы); метрики: доля времени в коридорах, энергопотребление, число коррекций и их типы.
- **Кандидат:** контур самонаблюдения по Винеру (ALGORITHM-ATLAS-WAVE5B §36): отрицательная обратная связь — отклонение метрики от коридора запускает минимальную коррекцию из версионируемого меню (перебалансировка бюджетов рядов, понижение фрагмента, смена расписания); каждая коррекция — событие с witness (метрика, отклонение, действие, результат); контроль — тот же контур с отключенной коррекцией (только телеметрия).
- **Протокол:** 5 сидов × 2 профиля; прежде чем корректировать механизм, контур проверяет целостность сигнала (дисциплина телеметрии); отрицательный результат фиксируется в reports/ (конвенция FR-15).
- **Критерии H-022.**

### EXP-003 — Механизмы обучения как producers BIR
- **Пререгистрация:** среда GridWorld→крафт-граф; бюджет 10⁶ шагов/механизм; метрики: образцы до порога, wall-clock, байт/структуру, точность, F1.
- **Механизмы:** автоматы Цетлина; ГА; трёхфакторное правило.
- **Правило решения:** SPEC-002 FR-C1 (зафиксировано здесь до запуска).

### EXP-004 — Ricci-fingerprint детектор деградации
- **Пререгистрация:** сценарии деградации (случайное удаление 5% рёбер; целенаправленное удаление мостов); нормальное окно — недельное пополнение графа; метрика ROC/AUC [^медицина]; сравнение с Recall@5 golden-набором как референс-детектором.
- **Протокол:** SPEC-003 FR-5. Дополнительно EXP-004b: curriculum-ordering vs случайный порядок (SPEC-003 FR-6).

### EXP-005 — Developmental Loop vs случайная выборка задач
- **Пререгистрация:** 50 этюдов крафта (зафиксированный набор); метрики: время до решения, transfer rate, доля решённых; контроль: случайная выборка задач, тот же агент.
- **Протокол:** SPEC-000 FR-7; scaffold fading обязателен (без fading эксперимент недействителен).

### EXP-006 — Guardrail из BIR-композиций
- **Пререгистрация:** вредоносный корпус (фиксированная выгрузка), невредоносный контрольный (тот же домен); метрики: TPR/FPR, p99 latency, контрпримеры BRC.
- **Базовые линии:** GPT-4-as-judge (внешний ориентир), TF-IDF классификатор.
- **Протокол:** этап 2 ROADMAP; SAT-доказательства противоречий обязательны в отчёте.

### EXP-023 — Операциональная асимметрия дуальности в суждениях (H-023)

- **Пререгистрация:** до запуска — зафиксировать: ≥3 синтетических корпуса суждений (≥200 суждений/корпус, сбалансированных по дуальной оси); ground truth полярность каждого суждения (мужское/женское/нейтральное); операциональное определение дуальной оси (бинарная классификация по культурным атрибутам); метрики: F-критерий, bootstrap CI 95% (n=10⁴), FDR-коррекция Бенджамини–Хохберга.
- **Протокол:** для каждого корпуса — частотный анализ распределения суждений по дуальной оси; F-критерий равномерности; bootstrap CI для effect size; FDR-коррекция на 3 корпуса; визуализация: bar plot с CI.
- **Базовые линии:** равномерное распределение (H₀); majority baseline (наиболее частый класс).
- **Риски:** артефакты синтетической генерации (балансировка не гарантирует отсутствия смещения); FDR-коррекция может быть избыточно консервативной при малом числе корпусов.
- **Критерии H-023.**

### EXP-024 — Рационализаторское зерно: RAT-индекс, конформность, accuracy (H-024)

- **Пререгистрация:** до запуска — зафиксировать: синтетический корпус ≥300 суждений с pre-decision confidence (Conf_pre ∈ [0,1]) и post-decision rationalization score (Rat_post ∈ [0,1]); задачу на конформность (социальное давление большинства, ≥20 trials); операциональное определение RAT-индекса = |Rat_post − Conf_pre| / max(Rat_post, Conf_pre); порог «высокого RAT» = 0.3 (median-split).
- **Протокол:** фаза 1 — измерение baseline confidence (без рационализации); фаза 2 — введение рационализации (участник обосновывает суждение пост-фактум); фаза 3 — задача на конформность; метрики: Spearman ρ (RAT ~ конформность), Δaccuracy (с рационализацией vs без), bootstrap CI 95%.
- **Базовые линии:** random ответ (accuracy 0.5 для бинарных суждений); условие без рационализации (baseline confidence).
- **Риски:** RAT-индекс чувствителен к шкале измерения Conf_pre/Rat_post; social desirability bias в задаче на конформность; синтетический корпус может не отражать реальную рационализацию.
- **Критерии H-024.**

### EXP-025 — Кросс-культурная воспроизводимость асимметрии (H-025)

- **Пререгистрация:** до запуска — зафиксировать: ≥3 синтетических корпуса, каждый репрезентирует различный культурный контекст (например: западный индивидуалистический, восточный коллективистский, ближневосточный религиозно-традиционный); ≥200 суждений/корпус, одинаковая процедура генерации; effect size = Cohen's h для пропорций; мета-анализ: random-effects модель (DerSimonian–Laird), I², forest plot.
- **Протокол:** повторить процедуру EXP-023 на каждом корпусе независимо; мета-анализ effect sizes; тест на гетерогенность (Cochran's Q + I²); проверка направления эффекта (знак Cohen's h); визуализация: forest plot.
- **Базовые линии:** H₀: нулевой общий эффект (мета-аналитический z-тест); prediction interval для оценки разброса.
- **Риски:** 3 корпуса — нижняя граница для мета-анализа (оценка I² нестабильна); синтетическая генерация «культурных контекстов» — риск отражения стереотипов, а не реальных различий.
- **Критерии H-025.**

### EXP-026 — Вектор рационализации vs онтологическая позиция (H-026)

- **Пререгистрация:** до запуска — зафиксировать: анкету онтологической позиции (≥10 пунктов, шкала Ликерта 1–5, essentialist vs constructionist); синтетический корпус ≥200 суждений с классификацией типа рационализации (натурализующая = essentialist-тип, социализирующая = constructionist-тип); one-hot кодирование: essentialist = [1,0], constructionist = [0,1]; метрика: cosine similarity между one-hot вектором рационализации и one-hot онтологической позицией.
- **Протокол:** фаза 1 — анкета онтологической позиции → бинаризация по медиане; фаза 2 — для каждого суждения: классификация типа рационализации; фаза 3 — расчёт cosine similarity, bootstrap CI 95% (n=10⁴), permutation test (n=10³) для проверки H₀: similarity = 0.
- **Базовые линии:** random assignment (shuffle онтологических позиций, permutation test); majority baseline.
- **Риски:** one-hot кодирование теряет нюансы онтологической позиции (дихотомия essentialist/constructionist — упрощение); синтетический корпус может искусственно усиливать корреляцию через процедуру генерации.
- **Критерии H-026.**

### EXP-027 — Нейрофизиологическая асимметрия и D-ось (H-027)

- **Пререгистрация:** до запуска — зафиксировать: 4 канонические задачи (mental rotation, word generation, face recognition, arithmetic) с синтетическими нейро-маркерами (accuracy, fluency, speed ∈ [0,1]); Laterality Index (LI) = (mean_right − mean_left) / (mean_right + mean_left); D-axis polarity = mean D1–D4; метрики: Spearman ρ, permutation test (n = 10⁴), FDR-коррекция на 4 задачи.
- **Протокол:** фаза 1 — генерация синтетических нейро-маркеров для n ≥ 200 синтетических «респондентов» с preregistered ground truth (известная корреляция ρ_gt); фаза 2 — измерение D-осевой полярности на тех же респондентах (EXP-023 протокол); фаза 3 — корреляционный анализ LI × D-axis; визуализация: scatter plot + regression line с bootstrap CI.
- **Базовые линии:** H₀: ρ = 0 (permutation test); baseline ρ из литературы (Hyde 2005: d < 0.35 → ρ ≈ 0.17).
- **Риски:** синтетические нейро-маркеры — прокси, не реальная нейрофизиология; малый n ограничивает мощность для слабых корреляций; важна проверка calibration: если ρ_gt в ground truth не восстанавливается → протокол не валиден.
- **Критерии H-027.**

### EXP-028 — Социо-экономическая модуляция RAT-индекса (H-028)

- **Пререгистрация:** до запуска — зафиксировать: синтетические контекстные переменные gini ∈ [0,1], education ∈ {low, mid, high}, income_quintile ∈ {Q1…Q5} для n ≥ 300 синтетических «респондентов»; RAT-индекс из EXP-024 протокола; метрики: двухфакторный ANOVA (Type III SS), partial η², FDR-коррекция на семейство из 3 тестов (gini, education, interaction).
- **Протокол:** фаза 1 — генерация синтетических респондентов с известным ground truth эффектом (η²_gt, preregistered); фаза 2 — измерение RAT-индекса по протоколу EXP-024; фаза 3 — ANOVA + post-hoc Tukey HSD; визуализация: interaction plot (RAT ~ gini × education).
- **Базовые линии:** H₀: все эффекты = 0; baseline main effect из Stanovich (2011): education → cognitive ability → rational thinking (d ≈ 0.4–0.6).
- **Риски:** синтетические gini/education — грубые прокси; RAT-индекс на синтетических данных ограничен по вариабельности; важна calibration: восстановление η²_gt из ground truth.
- **Критерии H-028.**

### EXP-029 — Культурная дистанция как предиктор D-оси (H-029)

- **Пререгистрация:** до запуска — зафиксировать: ≥30 синтетических корпусов, каждый с 5 Hofstede-аналогами (PDI, IDV, MAS, UAI, LTO ∈ [0,100]) и измеренным effect size H-023 (Cohen's h); target — знак effect size (±); метрики: 5-fold CV accuracy, AUC, binomial test vs chance (0.5), FDR-коррекция на 5+1 моделей.
- **Протокол:** фаза 1 — генерация синтетических корпусов с preregistered ground truth (известный вектор β для Hofstede → sign Cohen's h); фаза 2 — логистическая регрессия с 5 предикторами + interaction PDI × MAS; фаза 3 — 5-fold кросс-валидация (stratified по знаку effect size); визуализация: ROC-кривые, feature importance (permutation).
- **Базовые линии:** chance accuracy = 0.5 (биномиальный тест); baseline модель: только PDI + MAS (без остальных размерностей).
- **Риски:** n = 30 корпусов — малый размер для кросс-валидации; синтетические Hofstede-аналоги — упрощение; мультиколлинеарность размерностей (напр. PDI ~ IDV с отрицательной корреляцией в реальных данных).
- **Критерии H-029.**

### EXP-030 — Когортная стабильность D-осевой асимметрии (H-030)

- **Пререгистрация:** до запуска — зафиксировать: 5 синтетических когорт C1…C5 (1950, 1975, 2000, 2025, 2050_preregistered) с preregistered ground truth Cohen's h для каждой; ≥5 доменов суждений (random intercept в mixed-effects модели); метрики: mixed-effects model (lme4-стиль), cohort × time interaction, Cohen's d для межкогортных сравнений, bootstrap CI 95% (n = 10⁴), тест на линейный тренд.
- **Протокол:** фаза 1 — генерация синтетических когорт с известным ground truth (d_gt между C1 и C5, preregistered); фаза 2 — измерение D-осевой асимметрии (EXP-023 протокол) для каждой когорты; фаза 3 — mixed-effects model (Cohen's h ~ cohort + (1|domain)); фаза 4 — pairwise сравнения с поправкой Тьюки; визуализация: spaghetti plot (Cohen's h по когортам для каждого домена) + forest plot pairwise d.
- **Базовые линии:** H₀: d = 0 для всех межкогортных сравнений; baseline из литературы: Twenge (1997) — d ≈ 0.10–0.20 для гендерных различий между когортами 1970s vs 1990s.
- **Риски:** синтетические когорты — идеализация (конфаундинг возраст × период × когорта); C5 (2050) — чистая экстраполяция; малый размер эффекта (d ≥ 0.05) требует большой синтетической выборки (n ≥ 500/когорту для мощности 0.8).
- **Критерии H-030.**

### EXP-031 — Размер социальной группы и D-осевая асимметрия (H-031)
- **Пререгистрация:** до запуска — зафиксировать: 6 синтетических Dunbar-слоёв (L1=5, L2=15, L3=50, L4=150, L5=500, L6=1500) с preregistered контекстом суждения на слой; n ≥ 200 синтетических суждений на слой с известной D-полярностью; ground truth |Cohen's h| на слой с монотонным убыванием L1→L6 (ρ_gt = −0.5); метрики: Spearman ρ (номер слоя × |Cohen's h|), permutation test (n=10⁴), FDR-коррекция Бенджамини–Хохберга на 6 слоёв, тест монотонности соседних слоёв, калибровка восстановления ρ_gt.
- **Протокол:** для каждого слоя генерируется социальный контекст, реалистичный для его размера (L1: распределение домашних обязанностей; L6: голосование за национальную политику); измерение D-осевой асимметрии по протоколу EXP-023; корреляционный анализ слой × асимметрия; тест монотонности (знак ρ между соседними слоями одинаков); визуализация: line plot |Cohen's h| по слоям с bootstrap CI.
- **Базовые линии:** H₀: ρ = 0 (permutation test); слой-агностичная асимметрия (одинаковый |Cohen's h| на всех слоях).
- **Риски:** синтетические социальные контексты — идеализация (реальный индивидуум принадлежит всем слоям одновременно); Dunbar's number ≈ 150 — эмпирически оспариваемый параметр, дизайн проверяет связь, не само число; размер группы — не единственный фактор социальной структуры.
- **Критерии H-031.** Полный дизайн: docs/research/notes/DUALITY-socio-cognitive.md §1.

### EXP-032 — Рационализация как защита от когнитивного диссонанса (H-032)
- **Пререгистрация:** до запуска — зафиксировать: within-subject дизайн, n ≥ 200 синтетических респондентов; три условия диссонанса (low < 0.3; medium [0.3, 0.7); high ≥ 0.7), dissonance_score = 1 − cosine_similarity(judgment_vector, prior_belief_vector); ground truth — линейный рост RAT с dissonance_score (β_gt = 0.4); метрики: paired t-test (high vs low), Cohen's d для paired comparison, mixed-effects RAT ~ dissonance_score + (1 | respondent), bootstrap CI 95% (n=10⁴), FDR на 3 сравнения, калибровка β_gt.
- **Протокол:** для каждого респондента генерируется prior belief vector (D1–D4 по EXP-023 + синтетические измерения); для каждого условия — суждения с preregistered dissonance; Conf_pre измеряется до exposure к диссонансу, Rat_post — после; RAT-индекс по формуле EXP-024; визуализация: scatter RAT ~ dissonance_score с regression line и CI.
- **Базовые линии:** H₀: ΔRAT = 0 (Festinger-нуль); конкурирующая гипотеза Kahneman-постфактум (RAT не зависит от диссонанса).
- **Риски:** синтетический диссонанс без аффективного компонента (негативное возбуждение не моделируется); чувствительность RAT к шкалам Conf_pre/Rat_post; артефакты идеализированной within-subject генерации.
- **Критерии H-032.** Полный дизайн: docs/research/notes/DUALITY-socio-cognitive.md §2.

### EXP-033 — Онтологическая позиция как предиктор моральных оснований (H-033)
- **Пререгистрация:** до запуска — зафиксировать: 6 синтетических moral foundations (care, fairness, loyalty, authority, sanctity, liberty; endorsement ∈ [0,1] каждое); онтологическая позиция one-hot (essentialist [1,0] / constructionist [0,1], медианный split по протоколу EXP-026); target — бинарный endorsement (high/low, медианный split) на foundation; метрики: 5-fold stratified CV, multi-output логистическая регрессия (6 классификаторов), accuracy и AUC на foundation, binomial test vs chance (0.5), FDR на 6 foundations, preregistered контраст binding (loyalty+authority+sanctity) vs individualising (care+fairness+liberty), калибровка восстановления β_gt.
- **Протокол:** генерация синтетических респондентов с известным β_gt (градиент essentialism → binding); фаза 1 — анкета онтологической позиции (EXP-026); фаза 2 — измерение endorsement по 6 основаниям; фаза 3 — multi-output классификация с CV; визуализация: ROC на foundation, feature importance (permutation).
- **Базовые линии:** chance accuracy = 0.5 (binomial test); модель без онтологической позиции (только marginal base rates endorsement).
- **Риски:** синтетические MFT-шкалы — упрощение (Moral Foundations Questionnaire многомерен); n ≈ 200 при 6 выходах и CV — риск переобучения; дихотомия essentialist/constructionist теряет нюансы; генерация может искусственно усилить binding-градиент.
- **Критерии H-033.** Полный дизайн: docs/research/notes/DUALITY-socio-cognitive.md §3.

### EXP-034 — Нейропластичность как модератор когортного сдвига D-оси (H-034)
- **Пререгистрация:** до запуска — зафиксировать: 5 синтетических когорт (C1=1950, C2=1975, C3=2000, C4=2025, C5=2050_preregistered) с preregistered plasticity_score (0.30 / 0.55 / 0.70 / 0.85 / 0.90, монотонный рост); D-осевая асимметрия (Cohen's h) на когорту по протоколу EXP-030, ≥5 доменов суждений (random intercept); ground truth — interaction β_gt = −0.35; метрики: mixed-effects Cohen's h ~ cohort × plasticity_score + (1 | domain) (REML, Kenward-Roger df), standardised β interaction + bootstrap CI 95% (n=10⁴), Cohen's d контраста high-plasticity (C4+C5) vs low-plasticity (C1+C2), simple slopes plasticity → Cohen's h в каждой когорте, калибровка β_gt.
- **Протокол:** генерация D-асимметрии как функции cohort × plasticity с preregistered ground truth; оценка mixed-effects модели; тест простых эффектов по когортам; визуализация: interaction plot (Cohen's h ~ cohort при уровнях plasticity).
- **Базовые линии:** H₀: interaction β = 0; модель только с cohort (как в EXP-030, без модератора).
- **Риски:** plasticity_score — прокси на уровне когорты (реальная нейропластичность измеряется индивидуально); конфаундинг plasticity × cohort × age неразличим в синтетическом дизайне; C5 — чистая экстраполяция; n = 5 когорт ограничивает мощность interaction-теста; направление эффекта (amplifier vs buffer) — exploratory, не preregistered gate.
- **Критерии H-034.** Полный дизайн: docs/research/notes/DUALITY-socio-cognitive.md §4.

## Что здесь не бывает

— Гипотез без опровержимого порога («система станет умнее» — не гипотеза).
— Экспериментов без карточки (CONSTITUTION VI.2).
— Маркетинговых формулировок («AGI», «не лжёт» — CONSTITUTION VI.1).