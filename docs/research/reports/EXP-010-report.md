# EXP-010 Report — Single-pass WNN (WiSARD) vs TsetlinTrainer

**Статус: running** · Гипотеза: H-010 (`docs/research/HYPOTHESES.md`) · Продуценты: `io.matrix.tsetlin.{TsetlinTrainer,WisardProducer}`

## Методика прогона 2026-08-25 (первичные измерения)
Харнесс: `matrix-core/src/test/java/io/matrix/tsetlin/Exp010ComparisonTest.java` — детерминированный синтетический датасет (seed 42): BITS=16 (10 информативных с классовым смещением p=0.7/0.3, 6 шумовых p=0.5), TRAIN=320 / TEST=80, EPOCHS=5.
Тайминг: `System.nanoTime` вокруг обучающего цикла, **single-run JVM — НЕ JMH-grade**; вердикты по гейтам H-010 выставляются только после полного протокола (несколько датасетов, повторы).

## Результаты (реальные измерения)

| Модель | Обучение, мс | Точность на holdout |
|---|---|---|
| TsetlinTrainer (20 клауз, S=4.0, 100 состояний) | **10.068** | **0.5875** |
| WisardProducer (8 RAM) | **1.854** | **0.9250** |

Наблюдение (preliminary, один датасет): WiSARD быстрее в обучении ≈5.43× и точнее на этой задаче; гейт H-010 «≤2 п.п. разрыва при ≥10× ускорении» по скорости пока не достигнут (5.43× < 10×).

## Ограничения
- Один синтетический датасет малого размера; отсутствие тюнинга nClauses/S под задачу для Tsetlin.
- Замер wall-clock одноразовый; JIT-прогрев не контролировался.

## Следующее действие
Расширить харнесс до сетки датасетов + JMH-инфраструктура; после этого — verdict по гейтам H-010 и запись статуса в HYPOTHESES.md.
