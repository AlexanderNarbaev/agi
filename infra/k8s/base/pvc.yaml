apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: matrix-snapshots
  namespace: matrix
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: standard
